# Hello React - Practical 1

## Use Vite to create a React app

## Use ESLint and Prettier

### Formatting

- Run formatter (writes changes): `npm run format`
- Check formatting (CI-friendly, exit non-zero on differences): `npm run format:check`

Prettier is included as a devDependency in the project.
